config.stopBubbling=true
lombok.addLombokGeneratedAnnotation=true
# Generate Jackson 3 (tools.jackson) annotations for @Jacksonized
lombok.jacksonized.jacksonVersion += 3
# Use standard null-check method, so branch coverage for these annotated fields is not counted
lombok.nonNull.exceptionType=JDK